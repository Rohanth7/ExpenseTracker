package com.example.expensetracker.ui.theme

import androidx.compose.ui.graphics.Color

// ── Dark warm editorial palette ───────────────────────────────────────────

// Surfaces (warm dark scale)
val Canvas      = Color(0xFF211F1B)   // page background — deep warm
val Paper       = Color(0xFF2A2823)   // card surface — slightly elevated
val Deep        = Color(0xFF171511)   // hero / CTA extra-dark surface

// Text
val Ink         = Color(0xFFF2EBD9)   // primary text — warm off-white
val InkSoft     = Color(0xFFDAD3C3)   // secondary text on hero
val Muted       = Color(0xFF8A8276)   // label / secondary text
val Whisper     = Color(0xFF5C5750)   // placeholder text

// Hairlines (cream with alpha)
val Hairline         = Color(0x1AF4F1EB)  // rgba(244,241,235, 0.10)
val HairlineSoft     = Color(0x0DF4F1EB)  // rgba(244,241,235, 0.05)

// Primary — jade green
val Jade        = Color(0xFF7DC9A5)   // primary action (FAB, CTA, confirm)
val JadeDeep    = Color(0xFF2A5E47)   // jade-tinted dark surface
val JadeSoft    = Color(0xFF1A3829)   // subtle jade background
val JadeInk     = Color(0xFFAEDDCA)   // pale jade text on dark

// Coral — errors, pending, over-budget
val Coral       = Color(0xFFEE9A6E)
val CoralSoft   = Color(0xFF3C211A)   // dark coral background

// Amber — highlights, busiest month
val Amber       = Color(0xFFD4B268)
val AmberSoft   = Color(0xFF302718)   // dark amber background

// Surface overlays (translucent warm cream)
val SurfaceOverlay08  = Color(0x14F4F1EB)  // 8% — hero button bg
val SurfaceOverlay06  = Color(0x0FF4F1EB)  // 6% — daily pace tile bg
